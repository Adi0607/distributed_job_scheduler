spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/jobscheduler
    username: postgres
    password: postgres
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    com.jobscheduler: DEBUG
